set timeout=0
set default=0

insmod efi_gop
insmod font

if loadfont ${prefix}/fonts/unicode.pf2 ; then
    set gfxmode=auto
    set gfxpayload=keep
    terminal_output gfxterm
fi

menuentry "MyOS" {
    multiboot2 /boot/myos.elf
    boot
}